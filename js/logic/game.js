const initialState = {
  answers: [],
  notes: 1,
  time: 300,
  screen: `welcome`,
  results: [0, 10, 20, 12, 16, 14, 15, 8, 5]
};

const gameState = Object.assign(initialState);

export default gameState;
